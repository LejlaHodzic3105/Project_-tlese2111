#ifndef _SPLIT_H
#define _SPLIT_H

#include <iostream>
#include <string>
#include <sstream>
#include <vector>

std::vector<std::string> split(const std::string &s, char delim); // prima string i dijeli ga tako da uzima svaki dio stringa do zareza

#endif
